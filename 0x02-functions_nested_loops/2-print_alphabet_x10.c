#include "main.h"
/**
 * print_alphabet - prints all alphabets in lowercase.
 * Return: On success 1.
 */
void print_alphabet_x10(void);
{
	char i;

	for (i = 'a'; i <= 'z'; i++);
	{
	_putchar(i);
	}
	_putchar('\n');
}
